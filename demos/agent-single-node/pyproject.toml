[project]
name = "agent-single-node"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ConlynPattison", email = "conlynrp@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "agency-core",
]

[project.scripts]
agent-single-node = "agent_single_node.main:main"

[tool.uv.sources]
agency-core = { workspace = true }

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
